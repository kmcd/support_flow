team_average_close:
  owner: support_flow (Team)
  type: Statistic::Close
  value: <%= 86400 * rand(1..5) %>
  
team_average_reply:
  owner: support_flow (Team)
  type: Statistic::Reply
  value: <%= 86400 * rand(1..5) %>
  
agent_average_close:
  owner: rachel (Agent)
  type: Statistic::Close
  value: <%= 86400 * rand(1..5) %>
  
agent_average_reply:
  owner: rachel (Agent)
  type: Statistic::Reply
  value: <%= 86400 * rand(1..5) %>
  
customer_average_close:
  owner: peldi (Customer)
  type: Statistic::Close
  value: <%= 86400 * rand(1..5) %>
  
customer_average_reply:
  owner: peldi (Customer)
  type: Statistic::Reply
  value: <%= 3600 * rand(1..5) %>
<% if @guide.errors.empty? %>
  $("button.saved").removeClass('hide');
  setTimeout(function(){ $("button.saved").addClass('hide') },3000)
<% else %>
  $("button.not-saved").removeClass('hide');
  setTimeout(function(){ $("button.not-saved").addClass('hide') },3000)
<% end %>

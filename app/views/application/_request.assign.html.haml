%li
  %i.fa.fa-user-plus.text-yellow

  .timeline-item
    %span.time
      %i.fa.fa-clock-o
      = timeline_date activity

    %h3.timeline-header
      - owner_link activity

      - if activity.recipient.present?
        assigned
        = link_to activity.recipient.name, agent_path(activity.recipient)
        to
      - else
        released

      - link_to_request activity
      - request_name activity
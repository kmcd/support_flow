.agents.row
  .description.col-md-offset-0.col-md-10.col-sm-10.col-sm-offset-0.col-xs-11
    %h3 Agent activity
    %br/
    
    - @activities.each do |activity|
      %p[activity]
        %span.mark= time_day activity
        %span.text-muted= month_year activity
        &rarr;
        = link_to activity.owner.name, agent_path(activity.owner)
        %span.text-muted= description(activity).html_safe
        = request_open(activity) if activity.key[/open/]

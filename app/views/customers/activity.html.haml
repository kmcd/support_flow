.customers.row
  .description.col-md-offset-1.col-md-9.col-sm-9.col-sm-offset-1.col-xs-11
    %h3 Customer activity
    %br/
    
    
    - @activities.each do |activity|
      %p[activity]
        %span.mark= time_day activity
        %span.text-muted= month_year activity
        &rarr;
        = link_to activity.owner.name, customer_path(activity.owner)
        %span.text-muted= description(activity).html_safe
        = request_open(activity) if activity.key[/open/]
